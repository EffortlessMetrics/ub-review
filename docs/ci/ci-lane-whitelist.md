# CI lane whitelist

CI lanes are repository policy surfaces. A lane should not be added only because it feels useful; it needs a proof obligation and lifecycle.

`policy/ci-lane-whitelist.toml` records lane metadata including workflow, job, tier, default-PR behavior, blocking status, runner, LEM estimate, owner, intent, failure mode, proof obligation, allowed triggers, review date, and expiry.
