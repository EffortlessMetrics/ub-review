# Specs

Specs define behavior, evidence, acceptance examples, implementation ownership,
CI proof, non-goals, and failure modes.

Specs do not own PR order. PR order belongs in `plans/`.

A good spec says what must happen, what must fail closed, which fixtures or
commands prove the behavior, and which claims become valid after the proof
passes. A spec is not a task dump or roadmap.
