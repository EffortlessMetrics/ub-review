+        ensure!(lease.schema == crate::artifacts::RESOURCE_LEASE_SCHEMA);
+        ensure!(receipt.revision.is_some());
+        ensure!(receipt.revision == lease.revision);
+        ensure!(receipt.id == lease.consumer);
+        ensure!(receipt.commands.len() == 1);
+        ensure!(receipt.commands[0].status == "passed");
+        ensure!(out.join("task_ledger_events.ndjson").is_file());
+
+        let snapshot: serde_json::Value = serde_json::from_slice(&fs::read(
+            out.join("review/task_ledger_snapshot.json"),
+        )?)?;
+        let task_id = proof_command_task_id("proof-request-test", "head")?;
+        let task = snapshot["tasks"]
+            .as_array()
+            .context("worker task snapshot array missing")?
+            .iter()
+            .find(|task| task["id"] == task_id.as_str())
+            .context("worker task missing from snapshot")?;
+        ensure!(task["source"] == "Worker");
+        ensure!(task["state"] == serde_json::json!({"ResourcesReleased": "Succeeded"}));
+        ensure!(
+            task["receipt"]
+                == serde_json::json!({
+                    "Created": {"reference": "proof_receipt.json#/commands/0"}
+                })
+        );
+        Ok(())
+    }
+
+
+    #[test]
+    fn worker_result_write_failure_releases_resources_with_failed_receipt() -> Result<()> {
+        let temp = tempfile::tempdir()?;
+        let out = temp.path().join("out");
+        fs::create_dir_all(out.join("proof_receipt.json"))?;
+        let args = WorkerArgs {
+            proof_request: temp.path().join("unused-request.json").display().to_string(),
+            root: temp.path().display().to_string(),
+            out: out.display().to_string(),
+            timeout_sec: 30,
+        };
+        let request = ProofRequestV2 {
+            schema: crate::artifacts::PROOF_REQUEST_V2_SCHEMA.to_owned(),
+            id: "proof-request-write-failure".to_owned(),
+            kind: ProofKind::FocusedTest,
+            target: "cargo test --locked worker_test".to_owned(),
+            claim_ids: vec!["claim-a".to_owned()],
+            requested_by: vec!["worker-test".to_owned()],
+            expected_interpretation: "receipt publication failure remains explicit".to_owned(),
+            priority: "high".to_owned(),
+            timeout_sec: 30,
+            status: "approved".to_owned(),
+            base: "a".repeat(40),
+            head: "b".repeat(40),
         };
 
-        // Canonical schema + identity fields present and non-empty.
-        assert_eq!(receipt.schema, "ub-review.proof_receipt.v1");
-        assert_eq!(receipt.base, "abc1234");
-        assert_eq!(receipt.head, "def5678");
-        assert_eq!(receipt.requested_by, vec!["tests-oracle".to_owned()]);
-        assert_eq!(receipt.request_ids, vec!["claim-7".to_owned()]);
-        assert_eq!(receipt.commands.len(), 1);
-        // stdout/stderr are artifact PATHS (not inline content) and the files
-        // exist on disk — the bounding + artifact contract the old ad-hoc
-        // worker receipt lacked.
-        let stdout_rel = &receipt.commands[0].stdout;
-        assert!(
-            stdout_rel.starts_with("proof/req-1/head/"),
-            "stdout must be an artifact path under proof/<id>/head/, got {stdout_rel}"
-        );
-        assert!(
-            out.join(stdout_rel).exists(),
-            "stdout artifact must exist on disk"
+        let error = run_worker_request_with_runner(
+            &args,
+            request,
+            false,
+            &mut |_root, _argv, _env, _timeout, stdout, stderr, observe_process| {
+                observe_process(CommandProcessObservation::Spawned);
+                fs::write(stdout, b"ok\n")?;
+                fs::write(stderr, b"")?;
+                Ok(CommandStatus {
+                    exit_code: Some(0),
+                    timed_out: false,
+                    success: true,
+                    reason: "completed".to_owned(),
+                    duration_ms: 1,
+                })
+            },
+        )
+        .err()
+        .context("receipt target directory must fail publication")?;
+        ensure!(format!("{error:#}").contains("publish standalone worker proof result"));
+        ensure!(out.join("task_ledger_events.ndjson").is_file());
+
+        let snapshot: serde_json::Value = serde_json::from_slice(&fs::read(
+            out.join("review/task_ledger_snapshot.json"),
+        )?)?;
+        let task_id = proof_command_task_id("proof-request-write-failure", "head")?;
+        let task = snapshot["tasks"]
+            .as_array()
+            .context("worker task snapshot array missing")?
+            .iter()
+            .find(|task| task["id"] == task_id.as_str())
+            .context("worker failure task missing from snapshot")?;
+        ensure!(task["state"] == serde_json::json!({"ResourcesReleased": "Succeeded"}));
+        ensure!(
+            task["receipt"]["CreationFailed"]["reason"]
+                .as_str()
+                .is_some_and(|reason| reason.contains("worker result publication failed"))
         );
-        assert!(receipt.commands[0].stdout != receipt.commands[0].stderr);
-        // Serialize the whole receipt — it must round-trip as the canonical
-        // schema (the same JSON shape the gate and finalize consume).
-        let json = serde_json::to_string(&receipt)?;
-        let back: ProofReceipt = serde_json::from_str(&json)?;
-        assert_eq!(back.id, "req-1");
-        assert_eq!(back.commands.len(), 1);
-        assert_eq!(back.commands[0].status, "passed");
         Ok(())
     }
-}
+ }
 
 #[derive(Debug, Serialize)]
 struct PostErrorReceipt {
@@ -9279,6 +9322,7 @@ index 1111111..2222222 100644
             },
             tasks,
             None,
+            ProofExecutionPhase::ModelRequest,
             |_root, argv, env, timeout, stdout, stderr, _observe_process| {
                 commands.push(super::test_parse::command_display_with_env(env, argv));
                 let is_base = stdout.to_string_lossy().contains("base-plus-tests");
@@ -9398,6 +9442,7 @@ index 1111111..2222222 100644
             },
             tasks,
             None,
+            ProofExecutionPhase::ModelRequest,
             |_root, argv, env, timeout, stdout, stderr, _observe_process| {
                 commands.push(super::test_parse::command_display_with_env(env, argv));
                 let is_base = stdout.to_string_lossy().contains("base-plus-tests");
@@ -9664,6 +9709,7 @@ index 1111111..2222222 100644
             },
             tasks,
             None,
+            ProofExecutionPhase::ModelRequest,
             |_root, argv, env, timeout, stdout, stderr, _observe_process| {
                 commands.push(command_display(argv));
                 let is_base = stdout.to_string_lossy().contains("base-plus-tests");
@@ -9814,6 +9860,7 @@ index 1111111..2222222 100644
             },
             tasks,
             None,
+            ProofExecutionPhase::ModelRequest,
             |_root, argv, env, _timeout, stdout, stderr, _observe_process| {
                 commands.push(command_display(argv));
                 let is_base = stdout.to_string_lossy().contains("base-plus-tests");
@@ -9914,6 +9961,7 @@ index 1111111..2222222 100644
             remaining,
             tasks,
             None,
+            ProofExecutionPhase::ModelRequest,
             |_root, argv, _env, _timeout, _stdout, _stderr, _observe_process| {
                 commands.push(command_display(argv));
                 Ok(CommandStatus {
@@ -10175,6 +10223,7 @@ index 1111111..2222222 100644
             },
             tasks,
             None,
+            ProofExecutionPhase::ModelRequest,
             |_root, argv, env, _timeout, stdout, stderr, _observe_process| {
                 commands.push(command_display(argv));
                 let is_base = stdout.to_string_lossy().contains("base-plus-tests");
@@ -10293,6 +10342,7 @@ index 3333333..4444444 100644
             },
             tasks,
             None,
+            ProofExecutionPhase::ModelRequest,
             |_root, argv, env, _timeout, stdout, stderr, _observe_process| {
                 commands.push(command_display(argv));
                 let is_base = stdout.to_string_lossy().contains("base-plus-tests");
@@ -10420,6 +10470,7 @@ index 3333333..4444444 100644
             300,
             &lease,
             None,
+            ProofExecutionPhase::ModelRequest,
             &mut runner,
             &mut prepare,
         )?;
@@ -10477,6 +10528,7 @@ index 3333333..4444444 100644
             300,
             &lease,
             None,
+            ProofExecutionPhase::ModelRequest,
             &mut runner,
             &mut prepare,
         )?;
@@ -10529,6 +10581,7 @@ index 3333333..4444444 100644
             300,
             &lease,
             None,
+            ProofExecutionPhase::ModelRequest,
             &mut runner,
             &mut prepare,
         )?;
@@ -10634,6 +10687,7 @@ index 3333333..4444444 100644
             300,
             &lease,
             None,
+            ProofExecutionPhase::ModelRequest,
             &mut runner,
             &mut prepare,
         );
@@ -10683,6 +10737,7 @@ index 3333333..4444444 100644
             },
             tasks,
             None,
+            ProofExecutionPhase::ModelRequest,
             |_root, _argv, _env, _timeout, _stdout, _stderr, _observe_process| {
                 bail!("proof command should not run without a lease")
             },
@@ -10728,6 +10783,7 @@ index 3333333..4444444 100644
             },
             tasks,
             None,
+            ProofExecutionPhase::ModelRequest,
             |_root, _argv, _env, _timeout, _stdout, _stderr, _observe_process| {
                 bail!("proof command should not run when proof budget is zero")
             },
diff --git a/src/proof/broker.rs b/src/proof/broker.rs
index dcf330f..fef4aaf 100644
--- a/src/proof/broker.rs
+++ b/src/proof/broker.rs
@@ -79,6 +79,7 @@ pub(crate) fn run_initial_diff_proof_broker_v0(
         budget,
         selection.test_tasks,
         task_ledger,
+        ProofExecutionPhase::InitialImpact,
         run_command_to_files_with_spawn_observer,
         prepare_base_plus_tests_worktree,
     )?;
@@ -795,6 +796,7 @@ where
         budget,
         selection.test_tasks,
         task_ledger,
+        ProofExecutionPhase::ModelRequest,
         test_runner,
         prepare_base_plus_tests,
     )?;
@@ -824,6 +826,7 @@ where
         remaining_budget,
         replan.build_tasks,
         task_ledger,
+        ProofExecutionPhase::ModelRequest,
         build_runner,
     )?;
     result.proof_receipts.extend(build_result.proof_receipts);
@@ -904,6 +907,7 @@ pub(crate) fn run_follow_up_proof_broker_v0(
         budget,
         selection.test_tasks,
         task_ledger,
+        ProofExecutionPhase::FollowUp,
         run_command_to_files_with_spawn_observer,
         prepare_base_plus_tests_worktree,
     )?;
@@ -933,6 +937,7 @@ pub(crate) fn run_follow_up_proof_broker_v0(
         remaining_budget,
         replan.build_tasks,
         task_ledger,
+        ProofExecutionPhase::FollowUp,
         run_command_to_files_with_spawn_observer,
     )?;
     result.proof_receipts.extend(build_result.proof_receipts);
@@ -1054,6 +1059,7 @@ pub(crate) fn run_follow_up_proof_broker_v0_with_runner<F, G>(
     budget: ProofBudget,
     tasks: Vec<FocusedTestTask>,
     task_ledger: Option<&ProofTaskLedger>,
+    execution_phase: ProofExecutionPhase,
     runner: F,
     prepare_base_plus_tests: G,
 ) -> Result<ProofBrokerResult>
@@ -1078,6 +1084,7 @@ where
         budget,
         tasks,
         task_ledger,
+        execution_phase,
         runner,
         prepare_base_plus_tests,
     )
diff --git a/src/proof/build.rs b/src/proof/build.rs
index ca457f6..618ca7e 100644
--- a/src/proof/build.rs
+++ b/src/proof/build.rs
@@ -21,6 +21,7 @@ pub(crate) fn run_focused_build_proof_tasks_with_runner<F>(
     budget: ProofBudget,
     tasks: Vec<FocusedBuildTask>,
     task_ledger: Option<&ProofTaskLedger>,
+    execution_phase: ProofExecutionPhase,
     mut runner: F,
 ) -> Result<ProofBrokerResult>
 where
@@ -51,7 +52,11 @@ where
             ));
             if let Some(ledger) = task_ledger {
                 ledger.decline_command(
-                    &ProofCommandTask::focused_build(&task, task_timeout_sec),
+                    &ProofCommandTask::focused_build(
+                        &task,
+                        task_timeout_sec,
+                        execution_phase,
+                    ),
                     TaskNonExecutionDisposition::Refused,
                     "dry-run; proof broker did not execute focused build",
                 )?;
@@ -75,7 +80,11 @@ where
             ));
             if let Some(ledger) = task_ledger {
                 ledger.decline_command(
-                    &ProofCommandTask::focused_build(&task, task_timeout_sec),
+                    &ProofCommandTask::focused_build(
+                        &task,
+                        task_timeout_sec,
+                        execution_phase,
+                    ),
                     TaskNonExecutionDisposition::Refused,
                     "profile allows zero focused build leases",
                 )?;
@@ -104,7 +113,11 @@ where
             ));
             if let Some(ledger) = task_ledger {
                 ledger.decline_command(
-                    &ProofCommandTask::focused_build(&task, task_timeout_sec),
+                    &ProofCommandTask::focused_build(
+                        &task,
+                        task_timeout_sec,
+                        execution_phase,
+                    ),
                     TaskNonExecutionDisposition::BudgetDeferred,
                     "focused build proof lease budget exhausted by runtime profile",
                 )?;
@@ -126,7 +139,11 @@ where
             "focused build proof lease granted by runtime profile",
         );
         let spec = focused_build_command_spec_for_task(&task);
-        let command_task = ProofCommandTask::focused_build(&task, task_timeout_sec);
