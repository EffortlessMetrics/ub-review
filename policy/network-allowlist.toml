schema_version = "1.0"

[[allow]]
path = "src/main.rs"
owner = "ub-review"
surface = "review_posting"
classification = "network_io"
reason = "Posting mode may submit one prepared GitHub Pull Request Review when explicitly configured."
covered_by = ["cargo test --workspace --all-targets --locked"]
