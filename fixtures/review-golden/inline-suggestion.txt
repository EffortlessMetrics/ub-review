case: inline-suggestion
diff shape: one exact source-local repair suggestion
review payload status: prepared
posts a review: true
event: COMMENT
inline comments: 1

=== pr review body ===
## Decision

- Needs reviewer attention before upstream: findings remain.

## Confirmed findings

- Avoid constructing a slice from the pointer when the length is zero.
=== end pr review body ===

=== inline comment 1 ===
src/buffer.rs:39 RIGHT
Avoid constructing a slice from the pointer when the length is zero.

```suggestion
let slice = if len == 0 { &[] } else { unsafe { core::slice::from_raw_parts(ptr, len) } };
```
=== end inline comment 1 ===

=== serialized GitHub post payload ===
{
  "event": "COMMENT",
  "body": "## Decision\n\n- Needs reviewer attention before upstream: findings remain.\n\n## Confirmed findings\n\n- Avoid constructing a slice from the pointer when the length is zero.\n",
  "comments": [
    {
      "path": "src/buffer.rs",
      "line": 39,
      "side": "RIGHT",
      "body": "Avoid constructing a slice from the pointer when the length is zero.\n\n```suggestion\nlet slice = if len == 0 { &[] } else { unsafe { core::slice::from_raw_parts(ptr, len) } };\n```"
    }
  ]
}
=== end serialized GitHub post payload ===
