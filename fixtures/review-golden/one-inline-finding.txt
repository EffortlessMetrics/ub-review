case: one-inline-finding
diff shape: one source-local unsafe ordering defect
review payload status: prepared
posts a review: true
event: COMMENT
inline comments: 1

=== pr review body ===
## Decision

- Needs reviewer attention before upstream: findings remain.

## Confirmed findings

- `from_raw_parts` runs before the zero-length guard, so a dangling pointer is used even though the slice is never read.
=== end pr review body ===

=== inline comment 1 ===
src/buffer.rs:39 RIGHT
`from_raw_parts` runs before the zero-length guard, so a dangling pointer is used even though the slice is never read.
=== end inline comment 1 ===

=== serialized GitHub post payload ===
{
  "event": "COMMENT",
  "body": "## Decision\n\n- Needs reviewer attention before upstream: findings remain.\n\n## Confirmed findings\n\n- `from_raw_parts` runs before the zero-length guard, so a dangling pointer is used even though the slice is never read.\n",
  "comments": [
    {
      "path": "src/buffer.rs",
      "line": 39,
      "side": "RIGHT",
      "body": "`from_raw_parts` runs before the zero-length guard, so a dangling pointer is used even though the slice is never read."
    }
  ]
}
=== end serialized GitHub post payload ===
