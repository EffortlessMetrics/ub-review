case: inline-and-summary-finding
diff shape: one anchored defect plus one cross-cutting route concern
review payload status: prepared
posts a review: true
event: COMMENT
inline comments: 1

=== pr review body ===
## Decision

- Needs reviewer attention before upstream: findings remain.

## Confirmed findings

- `from_raw_parts` runs before the zero-length guard, so a dangling pointer is used even though the slice is never read.
- `Pool::lease` retains the same construction outside this diff, so the changed fix does not cover every production route.
=== end pr review body ===

=== inline comment 1 ===
src/buffer.rs:39 RIGHT
`from_raw_parts` runs before the zero-length guard, so a dangling pointer is used even though the slice is never read.
=== end inline comment 1 ===
