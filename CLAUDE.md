# Claude instructions

Follow `AGENTS.md` and `docs/development/AGENT_PR_POLICY.md`.

Work in scoped PRs, keep verification artifacts meaningful, and avoid broad
silent policy changes. If a required exception is discovered, add a structured
policy receipt with owner, reason, classification, and expiry rather than using a
silent `allow`.
